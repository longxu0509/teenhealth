<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="springboot.mybatis.mapper.TTestStandardMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="springboot.mybatis.po.TTestStandard">
        <id column="id" property="id" />
        <result column="test_id" property="testId" />
        <result column="test_name" property="testName" />
        <result column="gender" property="gender" />
        <result column="age_group_id" property="ageGroupId" />
        <result column="test_result1" property="testResult1" />
        <result column="test_result2" property="testResult2" />
        <result column="score" property="score" />
        <result column="update_time" property="updateTime" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, test_id, test_name, gender, age_group_id, test_result1, test_result2, score, update_time, create_time
    </sql>

    <select id="getTestScore" resultType="java.lang.Integer">
        select score
        from t_test_standard
        where test_id = #{testId} and gender = #{gender} and age_group_id = #{age_group_id}
        and test_result1 &lt;= #{count}  AND test_result2 &gt;= #{count}
    </select>

    <select id="getMaxCount" resultType="java.lang.Long">
        select test_result1
        from t_test_standard
        where test_id = #{testId} and gender = #{gender} and age_group_id = #{age_group_id}
        and score = 100
    </select>

    <select id="getMinTime" resultType="java.lang.Long">
        select test_result2
        from t_test_standard
        where test_id = #{testId} and gender = #{gender} and age_group_id = #{age_group_id}
          and score = 100
    </select>

</mapper>
