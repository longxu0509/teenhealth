<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="springboot.mybatis.mapper.TUserEvalMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="springboot.mybatis.po.TUserEval">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="report_id" property="reportId" />
        <result column="eval_event_id" property="evalEventId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, report_id, eval_event_id, create_time, update_time
    </sql>
    <insert id="insertUserEval" parameterType="springboot.mybatis.po.TUserEval">
        insert into t_user_eval
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="userId != null">
                user_id,
            </if>
            <if test="reportId != null">
                report_id,
            </if>
            <if test="evalEventId != null">
                eval_event_id,
            </if>
            <if test="createTime != null">
                create_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id},
            </if>
            <if test="userId != null">
                #{userId},
            </if>
            <if test="reportId != null">
                #{reportId},
            </if>
            <if test="evalEventId != null">
                #{evalEventId},
            </if>
            <if test="createTime != null">
                #{createTime},
            </if>
        </trim>
    </insert>
    <select id="selectByUserID" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from t_user_eval where user_id=#{userID} ORDER BY create_time DESC limit 1
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from t_user_eval where user_id=#{userID} ORDER BY create_time DESC
    </select>

    <select id="selectByReportId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from t_user_eval where report_id=#{reportId}
    </select>

    <select id="selectLatest" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from t_user_eval where user_id=#{userID} ORDER BY create_time DESC limit 1
    </select>

</mapper>
